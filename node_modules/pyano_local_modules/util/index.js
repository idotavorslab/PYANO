const span = (s, cls = null) => cls
                                ? `<span class="${cls}">${s}</span>`
                                : `<span>${s}</span>`;
const strong = (s) => `<strong>${s}</strong>`;





module.exports = {
	span,
	strong,
};
