async function handleTxt(txtname, txtpath) {
	const StoreFns = require("./StoreFns");
	StoreFns.setNewTruthFile(txtpath);
}

async function onDrop(e) {
	require('electron').remote.getCurrentWindow().focus();
	let { name, path, size, type } = e.originalEvent.dataTransfer.files[0];
	console.log({ name, path, size, type });
	try {
		const StoreFns = require("./StoreFns");
		StoreFns.setNewTruthFile(path);
	} catch (e) {
		console.error(e);
		debugger;
	}
	/*if ("text".in(type))
		return await handleTxt(name, path);
	else
		return await Alert.small._warning({ title: 'Please drop a truth txt file', html: `Can't handle files of type: "${type}"` });
	*/
}

module.exports = { onDrop };
